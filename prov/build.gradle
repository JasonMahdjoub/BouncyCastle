dependencies {
    implementation project(':core')
    testImplementation group: 'com.unboundid', name: 'unboundid-ldapsdk', version: '6.0.10'
}

jar.archiveBaseName = "bcprov-$vmrange"



artifacts {
    archives jar
    archives javadocJar
    archives sourcesJar
}

